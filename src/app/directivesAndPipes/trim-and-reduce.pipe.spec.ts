import { TrimAndReducePipe } from './trim-and-reduce.pipe';

describe('TrimAndReducePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimAndReducePipe();
    expect(pipe).toBeTruthy();
  });
});
